@* @model List<PorjectManagement.Models.ViewModels.ProjectListVM>

<div class="col-md-2 bg-light border-end" style="min-height: 100vh; position: fixed; left: 0; top: 0; overflow-y: auto;">
    <div class="p-3" style="padding-top: 80px !important;">
        <h5 class="fw-bold mb-4">MENU</h5>
        
        <div class="nav flex-column">
            <a asp-controller="Home" asp-action="Index" class="nav-link text-dark px-3 py-2">
                Tổng quan
            </a>
            <a asp-controller="Project" asp-action="Index" class="nav-link text-dark px-3 py-2">
                Dự án của tôi
            </a>
            <a href="#" class="nav-link text-dark px-3 py-2">
                Nhóm của tôi
            </a>
        </div>

        <hr class="my-4" />

        <h6 class="fw-bold text-uppercase text-muted mb-3" style="font-size: 0.85rem;">WORKSPACE</h6>
        <div class="nav flex-column">
            @if (Model != null && Model.Any())
            {
                int index = 1;
                foreach (var project in Model)
                {
                    <a asp-controller="Workspace" 
                       asp-action="Details" 
                       asp-route-id="@project.ProjectId" 
                       class="nav-link text-dark px-3 py-2 d-flex align-items-center">
                        <span class="me-2 text-muted" style="min-width: 25px;">@(index++).</span>
                        <span class="text-truncate">@project.ProjectName</span>
                    </a>
                }
            }
            else
            {
                <small class="text-muted px-3">Chưa có project</small>
            }
        </div>
    </div>
</div> *@